# Evals

This tree does not ship eval transcripts. Local runs may write under
`results/`; those files stay untracked. The keep-or-drop test for the
catalog is [`docs/skill-existence-test.md`](../docs/skill-existence-test.md).
